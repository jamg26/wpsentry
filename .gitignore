jwp_runs/
wpjson.json
.jwp_history
__pycache__/
*.pyc
*.pyo

# Node
node_modules/
dist/
.env
.env.local
.env.production
.dev.vars

# Wrangler
.wrangler/
worker/dist/

# Wrangler (real config with your IDs — copy from wrangler.toml.example)
worker/wrangler.toml

# Cloudflare
.cloudflare/
tc_cookies.txt

# Session / cookies (contain auth tokens)
.session-cookies.txt

# Test session state (contains auth tokens — generate with: cd tests && npm run setup)
tests/storageState.json
tests/test-results/

# Internal docs (setup guides, compliance reports)
docs/

# Copilot skills (internal orchestration — not for public)
.github/skills/
